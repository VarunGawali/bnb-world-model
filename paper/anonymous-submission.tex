%File: anonymous-submission.tex
%
% AAAI 2027 Anonymous Submission.
% Requires the AAAI author kit files in this directory:
%     aaai2027.sty   aaai2027.bst
% (download from the same AAAI author-kit package as the template PDFs).
%
% Compile with:  pdflatex -> bibtex -> pdflatex -> pdflatex

\documentclass[letterpaper]{article}
\usepackage{aaai2027}  % AAAI Press style; anonymity comes from the author field

\usepackage{times}      % roman font
\usepackage{helvet}     % sans-serif font
\usepackage{courier}    % typewriter font
\usepackage[hyphens]{url}
\usepackage{graphicx}
\urlstyle{rm}
\def\UrlFont{\rm}
\usepackage{natbib}
\usepackage{caption}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}
\newtheorem{definition}{Definition}
\newtheorem{proposition}{Proposition}
\renewcommand{\qedsymbol}{}   % drop the end-of-proof square
\frenchspacing
\setlength{\pdfpagewidth}{8.5in}
\setlength{\pdfpageheight}{11in}

% AAAI required PDF metadata
\pdfinfo{
/TemplateVersion (2027.1)
}

% ---------------------------------------------------------------------------
\title{CutWorld: A Latent Dynamics Model for Unified Branch-and-Cut Control}
\author{Anonymous Submission}
\affiliations{Anonymous Affiliation}
% ---------------------------------------------------------------------------

\begin{document}
\maketitle

\begin{abstract}
Branch-and-Cut, the core of modern Mixed-Integer Programming (MIP) solvers, makes costly heuristic choices at every search node: which variable to branch on and which cutting planes to add. Existing learning-based methods score these decisions from single-node features, leaving them blind to their downstream effect on the search. We introduce \emph{CutWorld}, a latent dynamics model for Branch-and-Cut that forecasts the consequences of a decision in latent space, without extra LP solves. Because the model predicts how each variable's representation evolves under an action, the branching policy can be re-applied to imagined states, unrolling a branching sequence in latent space and scoring each candidate by a MuZero-style return with a cost-to-go estimate of remaining tree size. The same latent selects globally valid Gomory cuts, so a single unified model drives branching, cut selection, and node ordering while provably preserving optimality. Framing this as a first instance of learned dynamics models for optimization, we evaluate on Set Cover, reporting optimality, nodes, time, and cuts with ablations isolating each component.
\end{abstract}

% ---------------------------------------------------------------------------
\section{Introduction}

A modern optimization solver is, at its core, a sequential decision maker. To solve a single instance it makes millions of discrete choices---which variable to branch on, which cutting planes to add, which open node to explore next---each one steering an enormous combinatorial search, and each governed today by a hand-engineered heuristic. Model-based planning agents such as MuZero and Dreamer \citep{schrittwieser2020mastering,hafner2020dream} have shown that learning a model of environment dynamics---and planning by imagining the consequences of actions---transforms sequential decision making, from Atari and Go to continuous control. We bring this idea inside an optimization solver: \emph{CutWorld}, a latent dynamics model for the solver's most consequential decisions, those of Branch-and-Cut.

Mixed-Integer Programming (MIP) is a cornerstone of combinatorial optimization, and modern solvers attack it with Branch-and-Cut. Starting from a linear programming (LP) relaxation, B\&C repeatedly executes two core operations at each search tree node: adding cutting planes---valid inequalities that tighten the relaxation without removing integer solutions---and branching on a fractional variable to partition the search space.

Unfortunately, making high-quality decisions at every node is computationally expensive. The gold standard for branching,
\emph{strong branching}, tentatively solves the child LPs of each candidate variable to measure its bound improvement; cut selection faces an analogous trade-off when choosing among large pools of valid inequalities. To amortize these costs, recent machine learning approaches learn to imitate strong branching in a single forward pass, e.g., with graph neural networks \citep{gasse2019exact,khalil2016learning}, or use reinforcement learning to select cuts
\citep{tang2020reinforcement}. These methods are effective, but they share a
structural limitation: each scores a candidate \emph{from the current node
alone}, discarding the lookahead signal---the effect a decision has on the resulting subtree.

CutWorld closes this gap with a learned latent dynamics model that simulates the downstream consequences of candidate decisions in latent space, without executing actual LP solves. We encode each search node as a bipartite graph and train a causal Transformer to predict how this state, alongside its constituent variable embeddings, evolves under candidate actions. Because the model predicts fine-grained, per-variable representations rather than a static graph summary, the branching policy can be re-applied to imagined states, unrolling multi-step decision sequences entirely in latent space. Candidates are scored by a MuZero-style return that combines a learned per-step reward with a cost-to-go value estimating the remaining size of the search tree. A single unified model concurrently drives the solver's core discrete choices---variable branching, cut selection, and node ordering---influencing only which decisions the solver makes and in what order, never the bounds or feasibility tests, so exactness is preserved.

\paragraph{Summary of Contributions}
\begin{itemize}
  \item \textbf{A latent dynamics model for Branch-and-Cut.} We learn a
  solver's decision dynamics---the first instance of \emph{latent dynamics
  models for optimization}---so that branching and cutting are \emph{planned in
  imagination} rather than chosen by fixed heuristics, replacing expensive
  online LP solves with learned multi-step latent rollouts.

  \item \textbf{Unified control with exactness guarantees.} We show that a single latent model can simultaneously drive variable branching, node selection, and global cut propagation while strictly maintaining the solver's exact optimality guarantees.

  \item \textbf{A search-tailored objective and stable training.} We design a cost-to-go formulation targeting the solver's true objective, total explored nodes---that trains directly from non-depth-first search traces, stabilized via a grounded MuZero-style loss with latent overshooting.

  \item \textbf{Empirical validation.} On Set Cover across difficulty tiers we report the four metrics that determine solver quality---optimality, explored nodes, solving time, and cuts applied---with an ablation that isolates each planning component's contribution and a fair both-solved-subset comparison against SCIP and classical branching heuristics.
\end{itemize}

% ---------------------------------------------------------------------------
\section{Related Work}

Learning to make solver decisions is an active area \citep{bengio2021machine}.
The dominant approach is imitation: \citet{khalil2016learning} regress
strong-branching scores from hand-crafted features, and \citet{gasse2019exact}
represent a node as a bipartite constraint--variable graph and train a graph
network to imitate strong branching in a single forward pass. This amortizes
the cost of strong branching but discards the very signal that makes it
strong---the quality of the subtree each candidate ultimately induces. The
bipartite encoder of \citet{gasse2019exact} is the node representation we build
on. Reinforcement learning restores a longer horizon: \citet{scavuzzo2022learning}
train a branching policy with tree-structured returns, and
\citet{nair2020solving} scale neural MIP solving with learned diving and
branching. These methods optimize long-term reward, but remain \emph{model-free}:
they treat the solver as a black box and never learn how a decision reshapes the
subsequent search state.

Cut selection has been learned along the same lines. \citet{tang2020reinforcement}
cast it as a sequential decision problem solved with reinforcement learning, and
\citet{paulus2022learning} imitate a look-ahead expert that scores each cut by
the bound improvement it yields---but the look-ahead only produces training
labels; at inference the policy again predicts from the current node alone.
Learned large neighborhood search \citep{sonnerat2021learning} targets yet
another solver decision. Across branching and cutting alike, prior work scores a
candidate either myopically or through an explicit, expensive look-ahead that
the learned policy itself cannot invoke.

CutWorld instead draws on model-based reinforcement learning. MuZero
\citep{schrittwieser2020mastering} plans with a learned dynamics model without
reconstructing observations, and Dreamer \citep{hafner2020dream} learns behaviors
by imagination in latent space, using latent overshooting to keep multi-step
predictions faithful. We bring this inside Branch-and-Cut, combining the
bipartite representation of \citet{gasse2019exact} with a learned latent forward
model: rather than ranking candidates by a static score or paying for explicit
LP look-ahead, we \emph{imagine} the subtree a decision induces and recover
strong branching's look-ahead signal at the cost of a few cheap latent rollouts.
A single such model drives branching, cut selection, and node ordering, whereas
prior methods learn each decision in isolation.

% ---------------------------------------------------------------------------
\section{Background and Problem Formulation}

A (binary) Mixed-Integer Program minimizes a linear objective over integer
variables subject to linear constraints,
\begin{equation}
\min_{x}\ c^\top x \quad \text{s.t.}\quad A x \ge b,\ \ x \in \{0,1\}^n,
\end{equation}
with $c\in\mathbb{R}^n$, $A\in\mathbb{R}^{m\times n}$, $b\in\mathbb{R}^m$. Set
Cover is the case $A\in\{0,1\}^{m\times n}$, $b=\mathbf 1$.

Branch-and-Cut solves the LP relaxation at each node; if the solution is
fractional it either adds valid cutting planes $\alpha^\top x \ge \beta$ that
tighten the relaxation, or branches on a fractional variable $x_j$ into the
subproblems $x_j=0$ and $x_j=1$. The number of nodes explored before optimality
is proved is governed by these two choices.

We model the search as an MDP $(\mathcal S,\mathcal A,P,R,\gamma)$: a state
$s_t$ is a search node (its LP relaxation as a constraint--variable graph); an
action $a_t$ selects a fractional variable to branch on or a cut to add; the
transition $P$ is the solver's deterministic response (child LPs, tightened
relaxation); and the per-step reward $r_t$ is the dual-bound improvement, a
dense proxy for progress. The ultimate objective is to prove optimality in as
few explored nodes as possible.

The transition $P$ is realized by the solver itself---solving child LPs and
re-optimizing---so evaluating the downstream effect of an action costs as much
as taking it, which is why prior learned policies avoid look-ahead. CutWorld's
central idea is to learn a latent surrogate for $P$: an encoder maps states to
a latent, and a learned dynamics model predicts how that latent transitions
under an action, so a decision's consequences can be \emph{imagined} without
invoking the solver. This turns look-ahead from an expensive oracle into a cheap
forward pass, and the reward and node-count signals above become the learning
targets for the model's reward and cost-to-go heads (Method).

\begin{proposition}[Exactness]
\label{prop:exact}
For any branching variable chosen among the fractional candidates, any subset
of globally valid cuts added at any node, and any ordering of the open nodes,
Branch-and-Cut returns a global optimum. Consequently CutWorld preserves the
solver's exactness guarantee for every setting of the learned policy.
\end{proposition}
\begin{proof}[Proof sketch]
Branching on a fractional $x_j$ into $x_j=0$ and $x_j=1$ partitions the integer
feasible set exhaustively, so no integer solution is lost. A globally valid cut
is satisfied by every integer-feasible point, so adding it removes no feasible
solution. Node ordering changes only the sequence in which subproblems are
explored, not the bound-and-integrality pruning rule, hence not the final
incumbent. The learned model only selects among these correctness-preserving
options, so the incumbent at termination is a global optimum.
\end{proof}

Our validity requirement drives the cut design in the Method: we generate
Gomory fractional cuts, which are valid by construction, and let the model
\emph{select} among them rather than invent inequalities.

% ---------------------------------------------------------------------------
\section{Method}

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
  node distance=5mm and 9mm,
  box/.style={draw, rounded corners, align=center, minimum height=10mm,
              inner sep=3pt, font=\small},
  lat/.style={align=center, font=\small},
  heads/.style={draw, dashed, rounded corners, align=center,
                minimum height=10mm, inner sep=3pt, font=\footnotesize},
  arr/.style={-{Latex[length=2mm]}, thick},
]
\node[box] (inp) {B\&B node\\{\footnotesize bipartite}\\{\footnotesize graph}};
\node[box, right=of inp] (enc) {GATv2\\Encoder};
\node[lat, right=7mm of enc] (z) {$z,\{h_i\}$};
\node[box, right=7mm of z] (dyn) {Causal\\Transformer\\{\footnotesize $z_{t+1},h_i^{t+1}$}};
\node[box, right=of dyn] (plan) {Latent\\rollout\\{\footnotesize score $R(c)$}};
\node[box, right=of plan] (dec) {Branch /\\Cut\\decision};
\node[heads, below=8mm of z] (hd)
  {Heads:\ $\pi$ (policy)\ \textbar\ $V$ (value)\\
   $C$ (cost-to-go)\ \textbar\ $\rho$ (cut)};

\draw[arr] (inp) -- (enc);
\draw[arr] (enc) -- (z);
\draw[arr] (z) -- (dyn);
\draw[arr] (dyn) -- (plan);
\draw[arr] (plan) -- (dec);
\draw[arr] (z) -- (hd);
\draw[arr] (hd.east) -| (plan.south);
\draw[arr, dashed] (plan.north) to[out=60,in=120] node[above,font=\footnotesize]
      {imagine next step (re-apply $\pi$)} (dyn.north);
\end{tikzpicture}
\caption{\textbf{CutWorld.} A GATv2 encoder maps each Branch-and-Cut node to a
graph latent $z$ and per-variable embeddings $\{h_i\}$. A causal Transformer
predicts how both evolve after an action, so the policy can be re-applied to an
\emph{imagined} state and a branching sequence unrolled in latent space (dashed
loop). Candidates are scored by the rollout return $R(c)$ (Eq.~\ref{eq:return});
the same latent drives the value, cost-to-go, and cut-selection heads. The model
chooses only \emph{which} decisions the solver makes, never the bounds or
feasibility tests, so exactness is preserved (Proposition~\ref{prop:exact}).}
\label{fig:arch}
\end{figure*}

We view Branch-and-Cut as a sequential decision process. At each node $t$ of the
search the solver observes a state $s_t$ (the current relaxation), takes an action
$a_t$ (branch on a fractional variable, or add a cut), and transitions to the next
node. We learn (i) an encoder that maps $s_t$ to a latent state, (ii) a dynamics
model that predicts how that latent evolves under an action, and (iii) heads that
score decisions by rolling the dynamics forward (Figure~\ref{fig:arch}).
Throughout, $d$ denotes the embedding dimension.

\subsection{Node encoder}
Each node is a bipartite graph over the constraints and variables of the
relaxation: variable nodes carry LP-derived features (solution value, reduced
cost, bound and basis status), constraint nodes carry their features, and each
edge carries its coefficient together with two derived features, the
RHS-normalized value and the sign. A graph network with GATv2 attention
\citep{brody2022how} performs two-directional message passing, and a learned
attention readout pools the result:
\begin{equation}
\big(\{h_i\}_{i=1}^{n},\, z\big) = \mathrm{Enc}(s_t),
\qquad h_i, z \in \mathbb{R}^{d},
\end{equation}
where $h_i$ is the embedding of variable $i$ and $z$ is a graph-level summary.
The embedding of an action that branches on variable $v$ is $a = h_v$.

\subsection{Latent dynamics}
A causal Transformer models the evolution of the latent along a trajectory. From
the sequence of tokens $u_\tau = W_{\mathrm{in}}[z_\tau \,\|\, a_\tau]$
($\tau \le t$) with learned positional encodings, it predicts the next graph
latent, while a per-variable head predicts how each variable embedding changes:
\begin{align}
z_{t+1} &= W_{\mathrm{out}}\,\mathrm{TF}(u_{0:t}), \\
h_i^{\,t+1} &= \mathrm{LN}\!\big(h_i^{\,t} + \mathrm{MLP}([\,h_i^{\,t}\,\|\,z_{t+1}\,\|\,a_t\,])\big).
\label{eq:varupdate}
\end{align}
Predicting per-variable embeddings~\eqref{eq:varupdate}, rather than only the
graph summary $z$, is what lets the branching policy be re-applied to an
\emph{imagined} state during planning. We ground the latent with a linear head
$\hat b_{t+1}=w_b^\top z_{t+1}$ that predicts the node's normalized dual bound,
tying it to a real solver quantity, and a reward head
$\hat r_{t}=w_r^\top z_{t+1}$ that predicts the per-step dual-bound improvement.

\subsection{Value, cost-to-go, and policy}
Let $\bar h_{\mathrm{frac}}$ be the mean embedding of the currently fractional
variables. The value and cost-to-go heads share this enriched input:
\begin{align}
V(z) &= \mathrm{MLP}_V([z \,\|\, \bar h_{\mathrm{frac}}]), \\
C(z) &= \mathrm{softplus}\big(\mathrm{MLP}_C([z \,\|\, \bar h_{\mathrm{frac}}])\big),
\end{align}
where $C$ predicts $\log(1+\text{remaining nodes})$. Its regression target is the
Monte-Carlo return $T-t$ read directly off a length-$T$ trajectory---the number of
steps still to come---which requires \emph{no} assumption that the trajectory is
depth-first ordered, unlike a subtree-size target. The branching policy is a
pointer network scoring each variable against the graph latent,
$\pi(v)\!\propto\! w^\top\!\tanh(W_k h_v + W_z z)$.

\subsection{Planning rollout for candidate selection}
To branch, we take the policy's top-$k$ candidates and score each by imagining the
subtree it induces. Starting from the current $(z,\{h_i\})$ with first action
$a=h_c$, we unroll the dynamics for $K$ steps, re-applying the policy to each
predicted state to choose the next action
$v_j=\arg\max_{v\in\mathcal{F}}\pi(v\mid z_j,\{h^j\})$ over the fractional set
$\mathcal{F}$. The candidate is scored by a MuZero-style return that accumulates
predicted rewards along the imagined path, bootstraps with the value at the leaf,
and penalizes predicted remaining work:
\begin{equation}
R(c) = \sum_{j=1}^{K}\gamma^{\,j-1}\hat r_j \;+\; \gamma^{\,K-1}V(z_K)
       \;-\; w_c\sum_{j=1}^{K}\gamma^{\,j-1}\hat C(z_j).
\label{eq:return}
\end{equation}
We expand a shallow tree of branching factor $b$ (the top-$b$ actions at each
step, child returns averaged); $b\!=\!1$ recovers a single greedy rollout.
Rather than branching on $\arg\max_c R(c)$, which lets a single noisy rollout
override a confident policy, we use a \emph{policy-anchored} rule: over the
candidate set we standardize the policy scores and the returns and select
$c^\star=\arg\max_c\big[\hat\pi(c)+\lambda\,\hat R(c)\big]$, so the latent
lookahead refines the policy's ranking instead of replacing it ($\lambda\!=\!0$
recovers the policy). A short horizon is used because deeper rollouts compound
dynamics error, and the rollout is invoked only when the policy is
uncertain---if the top candidate's probability exceeds a threshold, the policy
choice is taken directly---so the lookahead cost is paid only on genuinely close
decisions. The same value drives \emph{node selection}: open nodes are ordered
by ascending predicted cost-to-go $C(z)$, so the subtree expected to close
soonest is explored first. Because these choices affect only search order, never
the bounds or feasibility tests, exactness is preserved.
Algorithm~\ref{alg:select} summarizes candidate selection.

\begin{algorithm}[t]
\caption{Branching-variable selection by latent rollout}
\label{alg:select}
\begin{algorithmic}[1]
\State \textbf{Input:} node latent $z$, variable embeddings $\{h_i\}$,
fractional set $\mathcal{F}$, depth $K$, factor $b$
\State $\mathcal{C}\gets \textsc{TopK}_k\big(\pi(\cdot\mid z,\{h_i\}),\,\mathcal{F}\big)$
\For{each candidate $c\in\mathcal{C}$}
  \State $R(c)\gets \textsc{Rollout}(z,\{h_i\},c,K,b)$ \Comment{Eq.~\eqref{eq:return}}
\EndFor
\State \textbf{return} $\arg\max_{c\in\mathcal{C}} R(c)$
\end{algorithmic}
\end{algorithm}

\subsection{Cutting-plane selection}
The model performs cut \emph{selection}; validity is guaranteed by the
generator, not the learner. We generate a pool of Gomory fractional cuts at the
root from the LP optimal basis. Writing the relaxation in standard form---all
variables nonnegative, with the box $0\le x\le 1$ lifted into explicit rows
$x_j+t_j=1$ so every nonbasic variable rests at its lower bound---each basic
structural variable $x_j$ with a fractional value $\bar b$ and tableau row
$x_j+\sum_{k\in\mathcal N}\alpha_k v_k=\bar b$ yields the valid inequality
$\sum_{k\in\mathcal N}\mathrm{frac}(\alpha_k)\,v_k \ge \mathrm{frac}(\bar b)$,
which we express in the structural variables by substituting the slack
definitions. Derived from globally valid rows with no branching bounds applied,
these cuts hold for \emph{every} integer-feasible solution and are safely
inherited by all descendants of the node---genuine Branch-and-Cut rather than
cut-and-branch. A pointer head scores each candidate cut $\ell$ (with feature
vector $\phi_\ell$: violation, efficacy, density, parallelism, objective cutoff,
support fraction) against the node latent,
$\rho_\ell = w^\top\tanh(W_k\,\mathrm{MLP}(\phi_\ell)+W_z z)$, and cuts with
$\sigma(\rho_\ell)>\tau$ are added, replacing fixed heuristics such as maximum
violation.

\subsection{Learning objectives}
Each component has a supervised objective:
\begin{itemize}
  \item \textbf{Policy:} masked cross-entropy against the strong-branching
  choice over the candidate set.
  \item \textbf{Value:} Huber loss against the normalized dual bound.
  \item \textbf{Dynamics:} MSE-plus-cosine latent prediction on $z_{t+1}$, a
  reconstruction loss on $h_i^{\,t+1}$, grounding and reward losses on $\hat b$
  and $\hat r$, and a \emph{latent-overshooting} loss unrolling $K$ steps from
  the model's own predictions to curb compounding error.
  \item \textbf{Cost-to-go:} Huber loss on $\log(1{+}(T{-}t))$, with a
  value-consistency term $\lVert V(\tilde z)-V(z)\rVert^2$ aligning the value
  head to dynamics-predicted latents $\tilde z$.
  \item \textbf{Cut:} weighted binary cross-entropy against a cut-quality label.
\end{itemize}

% ---------------------------------------------------------------------------
\section{Experiments}

\subsection{Setup}
We evaluate on Set Cover instances generated with Ecole
\citep{prouvost2020ecole} across easy, medium, and hard difficulty tiers, and we
collect strong-branching trajectories for training. We compare against a
spectrum of branching rules: the trivial \emph{random} and \emph{most-fractional}
heuristics, the classical \emph{pseudocost} rule, \emph{full strong branching}
(the near-optimal but expensive oracle our policy imitates), and \emph{SCIP}'s
default reliability-pseudocost branching (the strong solver baseline). We also
report an \emph{imitation} policy---the learned policy without any latent
lookahead---as the prior learning-based approach. We report four
metrics per method on a common held-out instance set: the fraction of instances
closed to \emph{optimality} within the time limit, the number of explored
\emph{nodes}, the wall-clock solving \emph{time}, and the number of
\emph{cutting planes} applied. Optimality is reported first because a reduction
in nodes is only a genuine improvement when the instance is actually solved: a
method that is slow per node can appear to explore fewer nodes merely by timing
out. Accordingly, all node and time comparisons are made on the subset of
instances solved to optimality by \emph{both} the method and the baseline, which
isolates decision quality from the timeout confound.

\subsection{Training details}
The objectives above are optimized in a five-stage curriculum so that each
component builds on trained predecessors: (1) policy (encoder and policy head);
(2) value (encoder and policy frozen); (3) dynamics (encoder frozen); (4) joint
fine-tuning of all components with the cost-to-go and value-consistency terms;
and (5) cut selection. Class imbalance in the leaf and cut labels is corrected
with a positive-class weight equal to the negative/positive ratio, and early
stopping on a validation metric selects each stage's best checkpoint. Full
hyperparameters are listed in the appendix.

\subsection{Ablation}
To understand where any improvement comes from, we ablate the planner one
component at a time, starting from the imitation policy alone and successively
enabling the value-based rollout, the cost-to-go objective, the tree-structured
rollout, and the reward-based return.

\subsection{Results}
Table~\ref{tab:main} reports the four metrics on the medium tier ($30$
instances). Node counts are heavy-tailed, so we give both the median and the
mean$\pm$std. The learned policy explores far fewer nodes than the naive
heuristics (random, most-fractional) and closes more instances, but it does not
match the pseudocost rule, SCIP, or strong branching; strong branching attains
the fewest nodes but is the slowest. Among the learned variants, the full model
(reward return) does not reduce the median below the imitation policy, but it
has the lowest variance---its worst-case node count is markedly smaller
(max.\ $3437$ vs.\ $5548$ for imitation)---so the latent lookahead trims the
tail rather than the median. All node and time comparisons are on the
both-solved subset.

\begin{table}[t]
\centering
\small
\begin{tabular}{lccccc}
\toprule
Method & \%\,Opt. & Median & Nodes (mean$\pm$std) & Time & Cuts \\
\midrule
Strong branching & 97  & \textbf{13}  & \textbf{18}\,$\pm$\,21   & 20.6 & 26.9 \\
SCIP             & 100 & 41           & 104\,$\pm$\,230          & 9.2  & 25.4 \\
Pseudocost       & 100 & 229          & 304\,$\pm$\,381          & 7.1  & 25.3 \\
Most-fractional  & 93  & 1585         & 3105\,$\pm$\,3855        & 29.4 & 25.6 \\
Random           & 93  & 2379         & 4415\,$\pm$\,4947        & 36.0 & 24.3 \\
\midrule
Imitation (policy) & 100 & 397        & 699\,$\pm$\,1032         & 16.1 & 23.9 \\
\ + value rollout  & 100 & 462        & 654\,$\pm$\,958          & 18.2 & 25.4 \\
\ + cost-to-go     & 100 & 430        & 709\,$\pm$\,990          & 18.3 & 24.8 \\
\ + tree rollout   & 100 & 444        & 787\,$\pm$\,1253         & 19.4 & 24.4 \\
\textbf{Ours (full)} & 100 & 484      & \textbf{626\,$\pm$\,690} & 17.5 & 25.6 \\
\bottomrule
\end{tabular}
\caption{Medium tier ($500\times1000$ Set Cover, $30$ instances). \%\,Opt.\ is
the fraction closed to optimality; nodes and time are on the both-solved subset.
The learned policy beats the naive heuristics but not pseudocost, SCIP, or
strong branching; our full model attains the lowest node variance among the
learned variants. Cuts are SCIP's internal separators (equal across branching
rules). Easy-tier results, where all strong methods solve in $\le\!2$ nodes,
are in the appendix; the hard tier ($1000\times2000$) is intractable within the
time budget for all methods, including SCIP.}
\label{tab:main}
\end{table}

% Generated by tools/make_plots.py; guarded so the paper compiles before the
% figure files exist. Uncomment the \ref sentence once the PDFs are present.
\IfFileExists{figures/fig_ablation_nodes.pdf}{%
\begin{figure}[t]
\centering
\includegraphics[width=0.49\linewidth]{figures/fig_ablation_nodes.pdf}\hfill
\IfFileExists{figures/fig_solved.pdf}
  {\includegraphics[width=0.49\linewidth]{figures/fig_solved.pdf}}{}
\caption{Explored nodes (median, log scale) and optimality rate by branching
rule. Node counts are compared on the both-solved subset.}
\label{fig:results}
\end{figure}}{}

\subsection{Cut selection}
We isolate the learned cut-selection head in our own neural Branch-and-Cut
solver, where the model controls which of the (globally valid) root Gomory cuts
are added. All three modes---no cuts, max-violation selection, and learned
selection---draw from the \emph{same} valid cut pool, so the comparison isolates
the selection rule; every solve is verified to reach the true optimum (matching
SCIP). Table~\ref{tab:cuts} reports mean explored nodes over 30 Set Cover
instances under a cut budget of one and two cuts per node.

\begin{table}[t]
\centering
\begin{tabular}{lcc}
\toprule
Selection rule & Nodes ($\le\!1$ cut) & Nodes ($\le\!2$ cuts) \\
\midrule
No cuts        & 7.73          & 7.73          \\
Max-violation  & 7.07 ($-8.6\%$)  & 6.87 ($-11.2\%$) \\
Learned        & \textbf{6.80} ($-12.1\%$) & 6.87 ($-11.2\%$) \\
\bottomrule
\end{tabular}
\caption{Cut-selection ablation on Set Cover ($30$ instances, all closed to
optimality). Valid cuts reduce explored nodes, and under the tightest budget of
a single cut per node the learned head selects a more effective cut than the
max-violation heuristic ($-12.1\%$ vs.\ $-8.6\%$); as the budget loosens both
rules capture most of the benefit and converge.}
\label{tab:cuts}
\end{table}

\subsection{Discussion}
Three findings stand out. First, the learned policy is a competent branching
rule: it explores far fewer nodes than the naive heuristics and closes more
instances, and it is markedly faster than the strong-branching oracle it
imitates, confirming that the GNN amortizes strong branching into a cheap
forward pass. Second, it nonetheless does not match the pseudocost rule or SCIP,
whose reliability branching, presolving, and primal heuristics remain ahead;
closing this gap is a matter of policy quality rather than planning, and points
to reinforcement-learning fine-tuning with tree-structured returns
\citep{scavuzzo2022learning} as the natural next step. Third, the latent
lookahead does not lower the median node count below the imitation policy, but
it reduces the variance and the worst-case node count, indicating that the
learned dynamics is reliable enough to \emph{avoid} poor branches but not yet
accurate enough to consistently \emph{improve} on a confident policy---hence the
policy-anchored, confidence-gated design, and the value of a more faithful
dynamics model as future work. In contrast, learned cut \emph{selection} yields
a consistent gain over the max-violation heuristic (Table~\ref{tab:cuts}),
suggesting that latent evaluation is presently more useful for scoring a small
pool of candidate cuts than for re-ranking branching variables. Together these
results map where a learned latent model does and does not yet help inside an
exact solver, while preserving optimality throughout.

\subsection{Reproducibility}
Experiments were run on NVIDIA Quadro RTX 5000 (16\,GB) GPUs under CUDA~13.2,
using PyTorch and PyTorch Geometric. All seeds, hyperparameters, and source code
are provided.

% ---------------------------------------------------------------------------
\section{Conclusion}
We have presented a latent dynamics model for Branch-and-Cut that plans the solver's
discrete decisions---branching, node selection, and the selection of globally
valid cutting planes---by imagining their consequences in latent space rather
than computing them with LP solves, all while preserving the solver's optimality
guarantee. We view this as the first realized instance of a broader agenda:
learned learned dynamics models for optimization, in which a solver's decision dynamics are
modeled so that its choices can be planned rather than guessed by fixed
heuristics. Branch-and-Cut exercises the hardest version of that idea---planning
inside an exact search tree under a strict correctness constraint---but the same
machinery applies wherever a solver makes a sequence of discrete choices.
Extending it to improvement search, such as learned large neighborhood search
where the latent dynamics model imagines the effect of re-optimizing a chosen subset of
variables, is a natural next step, and one where a forward model can be exploited
without the exactness constraints of tree search.

% ---------------------------------------------------------------------------
\bibliographystyle{aaai2027}
\bibliography{references}

% ---------------------------------------------------------------------------
\appendix
\section{Full Architecture}
\label{app:arch}
Figure~\ref{fig:arch-full} details the complete data flow, expanding the
overview of Figure~\ref{fig:arch} with the encoder internals, every prediction
head, the dynamics model, and the planning rollout. Two components---the
subtree-size head and a global-context projection---are implemented but disabled
in all experiments and are shown for completeness only.

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
  font=\footnotesize,
  stage/.style={draw, rounded corners, align=left, inner sep=5pt,
                text width=0.86\linewidth},
  arr/.style={-{Latex[length=2mm]}, thick},
]
\node[stage] (inp)
  {\textbf{Input: one Branch-and-Cut node (from SCIP).} Bipartite
   constraint--variable graph: variable features $[N_v,19]$, constraint
   features $[N_c,5]$, edges $[2,E]$ with edge features $[E,3]$ (coefficient,
   RHS-normalized value, sign).};
\node[stage, below=4mm of inp] (enc)
  {\textbf{Encoder --- BipartiteGNN.} Input projections
   $\mathrm{var\_proj}\!:\!19\!\to\!128$ and
   $\mathrm{con\_proj}\!:\!5\!\to\!128$. Three message-passing rounds, each
   applying GATv2 convolutions constraints$\to$variables then
   variables$\to$constraints (4 attention heads, edge features injected into
   every message), with ReLU, LayerNorm, and a residual. A CrossAttentionPool
   (a learned query attends over the variable embeddings) forms the graph
   latent. \emph{Outputs:} per-variable $\{h_i\}\in\mathbb{R}^{N_v\times128}$
   and graph latent $z\in\mathbb{R}^{128}$.};
\node[stage, below=4mm of enc] (hd)
  {\textbf{Prediction heads.} PolicyHead (pointer network)
   $\to$ branch scores $[N_v]$; ValueHead$([z\,\|\,\bar h_{\mathrm{frac}}])\to$
   normalized dual bound; CostToGoHead (softplus)
   $\to\log(1+\text{remaining nodes})$;
   IntegralityHead$([z\,\|\,\text{depth}\,\|\,n_{\mathrm{frac}}])\to
   P(\text{next is leaf})$, used to skip rollouts near leaves;
   CuttingPlaneHead (pointer network) $\to$ cut scores $[N_{\text{cuts}}]$.
   \emph{Disabled: SubtreeSizeHead and a global-context projection.}};
\node[stage, below=4mm of hd] (dyn)
  {\textbf{Dynamics --- DynamicsTransformer.} Token
   $u_t=W_{\mathrm{in}}[z_t\,\|\,a_t]+\text{pos}$, with $a_t$ the branched
   variable's embedding (a column of $\{h_i\}$). Four causal Transformer layers
   (masked self-attention + GELU feed-forward, $4\times$ width). Predicts the
   next graph latent $z_{t+1}$; a per-variable head sets
   $h_i^{t+1}=\mathrm{LN}(h_i+\mathrm{MLP}([h_i\,\|\,z_{t+1}\,\|\,a_t]))$.
   Grounding heads read the predicted latent: $\hat b(z_{t+1})$ (next dual
   bound) and $\hat r(z_{t+1})$ (per-step reward). A sliding token buffer
   (capacity $512$) supports long solves.};
\node[stage, below=4mm of dyn] (plan)
  {\textbf{Planning --- latent rollout.} For each top-$k$ policy candidate $c$:
   branch on $c\to(z_1,\{h^1\})$, re-apply the policy on the predicted state to
   choose the next action, and roll forward $K$ steps with branching width $b$.
   Score $R(c)=\sum_{j}\gamma^{j-1}\hat r_j+\gamma^{K-1}V(z_K)
   -w_c\sum_{j}\gamma^{j-1}\hat C(z_j)$ and branch on $\arg\max_c R(c)$; open
   nodes are ordered by predicted cost-to-go $\hat C$.};

\draw[arr] (inp) -- (enc);
\draw[arr] (enc) -- (hd);
\draw[arr] (hd)  -- (dyn);
\draw[arr] (dyn) -- (plan);
\draw[arr] (enc.east) to[out=0,in=0]
      node[right,font=\scriptsize]{$z,\{h_i\}$} (dyn.east);
\draw[arr] (hd.west) to[out=180,in=180]
      node[left,font=\scriptsize]{$\pi,V,\hat C$ used in rollout} (plan.west);
\draw[arr, dashed] (plan.east) to[out=0,in=0]
      node[right,font=\scriptsize]{imagine next step} (dyn.east);
\end{tikzpicture}
\caption{Full CutWorld architecture. The encoder produces a graph latent $z$ and
per-variable embeddings $\{h_i\}$ that feed both the prediction heads and the
dynamics model. The dynamics model predicts the next latent and per-variable
embeddings under an action, and the planning rollout re-applies the policy to
these imagined states to score candidates. Disabled components are noted for
completeness.}
\label{fig:arch-full}
\end{figure*}

% ---------------------------------------------------------------------------
\section{Easy-Tier Results}
\label{app:easy}
Table~\ref{tab:easy} reports the easy tier ($200\times400$ Set Cover, $30$
instances). These instances are nearly trivial---SCIP and strong branching close
them in a single node on average---so node counts are dominated by noise and the
comparison is not informative; we include it for completeness. Every method
solves all instances to optimality.

\begin{table}[h]
\centering
\small
\begin{tabular}{lccccc}
\toprule
Method & \%\,Opt. & Median & Nodes (mean$\pm$std) & Time & Cuts \\
\midrule
Strong branching & 100 & 1  & 1.3\,$\pm$\,0.9   & 1.05 & 22.4 \\
SCIP             & 100 & 1  & 1.8\,$\pm$\,1.7   & 0.93 & 19.5 \\
Pseudocost       & 100 & 6  & 14.8\,$\pm$\,22   & 0.85 & 19.4 \\
Most-fractional  & 100 & 14 & 47.8\,$\pm$\,83   & 1.06 & 19.4 \\
Random           & 100 & 9  & 74.2\,$\pm$\,171  & 1.04 & 18.5 \\
\midrule
Imitation (policy) & 100 & 22 & 68.1\,$\pm$\,118 & 1.31 & 21.2 \\
\ + value rollout  & 100 & 8  & 60.3\,$\pm$\,104 & 1.67 & 20.6 \\
\ + cost-to-go     & 100 & 15 & 90.5\,$\pm$\,152 & 2.11 & 18.3 \\
\ + tree rollout   & 100 & 11 & 97.1\,$\pm$\,180 & 2.25 & 20.6 \\
Ours (full)        & 100 & 12 & 89.4\,$\pm$\,187 & 2.05 & 19.0 \\
\bottomrule
\end{tabular}
\caption{Easy tier ($200\times400$ Set Cover, $30$ instances). All methods solve
every instance; the instances are near-trivial (SCIP and strong branching
average $\le\!2$ nodes), so the node comparison is uninformative and included
only for completeness.}
\label{tab:easy}
\end{table}

\end{document}
